#!/bin/sh
# Reject AI attribution trailers. See CLAUDE.md §1.
if grep -qiE '^(co-authored-by|assisted-by|generated-by|claude-session):|generated with \[?claude code' "$1"; then
  echo "commit-msg: attribution trailer detected — remove it (see CLAUDE.md §1)." >&2
  exit 1
fi
exit 0
